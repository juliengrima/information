{% extends 'layout.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div id="main">
    <!-- One -->
    <section id="one">
        <header class="major">
            <h2>Edition de {{user.username}}</h2>
        </header>
    </section>
    <!-- Two -->
    <section id="two">
        <div class="row">
            <div class="button-new">
                <ul class="icons">
                    <li>
                        <a href="{{ path('app_user_index') }}" class="button">Retour à la liste</a>
                    </li>
                    <li>
                         {{ include('user/_delete_form.html.twig') }}
                    </li>
                </ul>
                
            </div>
           {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </section>
</div>  
{% endblock %}
