{% extends 'layout.html.twig' %}

{% block title %}Edition du téléphone{% endblock %}

{% block body %}
<div id="main">
    <!-- One -->
    <section id="one">
        <header class="major">
            <h2>Edition du numéro {{phone.number}}</h2>
        </header>
    </section>
    <!-- Two -->
    <section id="two">
        <div class="row">
            <div class="button-new">
                <ul class="icons">
                    <li>
                        <a href="{{ path('app_phone_index') }}" class="button">Retour à la liste</a>
                    </li>
                    <li>
                         {{ include('phone/_delete_form.html.twig') }}
                    </li>
                </ul>
                
            </div>
            {{ include('phone/_form.html.twig', {'button_label': 'Update'}) }} 
        </div>
    </section>
</div>  
{% endblock %}
