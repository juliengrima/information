{% extends 'layout.html.twig' %}

{% block title %}Modifier la Localisation{% endblock %}

{% block body %}
<div id="main">
    <!-- One -->
    <section id="one">
        <header class="major">
            <h2> Edition de l'adresse</h2>
        </header>
    </section>

    <!-- Two -->
    <section id="two">
        <div class="row">
            <div class="button-new">
                <a href="{{ path('app_localisation_index') }}" class="button">Retour à la liste</a>
            </div>
            {{ include('localisation/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('localisation/_delete_form.html.twig') }}
        </div>
     </section>
</div> 
{% endblock %}
